
import { logicHelper } from 'sa/helpers/logic-helper';
import { module, test } from 'qunit';

module('Unit | Helper | logic helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = logicHelper([42]);
  assert.ok(result);
});

